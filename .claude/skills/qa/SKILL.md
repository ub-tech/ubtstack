
---
name: qa
version: 3.0.0
description: Deprecated — QA is now part of /review.
---

# /qa has been merged into /review

As of the Option A pipeline revamp, QA validation is integrated into the `/review` command.

Run `/review` instead — it performs both code review and QA gate validation.

Reference materials in `qa/references/` and `qa/templates/` remain available for use by `/review`.
